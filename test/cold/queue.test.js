/**
 * work queue
 *
 * Rate limiter.
 *
 * Limit the parallelism to 4 concurrent calls?
 *
 * Benchmark to find nice numbers?
 *
 * Hide the vasync work queues inside the create, update, delete
 * calls so that the library using this doesn't have to
 * do their own throttling so intensely.
 *
 */
